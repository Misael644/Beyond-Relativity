if ArrayList < Count {
    if keyboard_check_pressed(vk_add) {ArrayList += 1};
    if ArrayList != 0 {
    if keyboard_check_pressed(vk_subtract) {ArrayList -= 1};
    };
};

if instance_exists(obj_ship){
yPos[Count] = obj_ship.speed};

if keyboard_check_pressed(ord("O")){
    SetRate = get_string("Set the line smoothness. 1 = Every frame, 60 = Every second","");
    SpawnRate = real(SetRate);
};

